### Class Diagram of the Movie SDK
```mermaid
  classDiagram
    class MovieSDK{
        +String clientSecret
        +String clientKey
        +String movieAccessKey
        +getAll()
        +getById()
        +getQuotes()
    }
```
